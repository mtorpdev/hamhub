# Local API against production PostgreSQL.
# Copy to .env.production.local and fill in the real value outside git.
ConnectionStrings__DefaultConnection='Host=prod-host;Database=hamhub;Username=hamhub;Password=secret;SSL Mode=Require;Trust Server Certificate=true'
ASPNETCORE_ENVIRONMENT=Development
